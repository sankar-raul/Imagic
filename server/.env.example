# Environment Variables for JWT Authentication

# Copy these to your .env file

# ============================================
# JWT Configuration (REQUIRED)
# ============================================

# JWT Secret Key - CHANGE THIS IN PRODUCTION!
# Must be at least 32 characters long for security
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars

# Access Token Expiration (default: 15 minutes)
# Format: 60s, 5m, 1h, 1d, etc.
JWT_EXPIRES_IN=15m

# Refresh Token Expiration (default: 7 days)
# Format: 60s, 5m, 1h, 1d, 7d, etc.
REFRESH_TOKEN_EXPIRES_IN=7d

# ============================================
# Server Configuration
# ============================================

# Server port
PORT=8080

# Environment mode
MODE=development

# ============================================
# Database Configuration (Your existing)
# ============================================

# MongoDB connection string
# MONGODB_URI=mongodb://localhost:27017/imagic

# ============================================
# Cloudinary Configuration (Your existing)
# ============================================

# CLOUDINARY_CLOUD_NAME=your_cloud_name
# CLOUDINARY_API_KEY=your_api_key
# CLOUDINARY_API_SECRET=your_api_secret

# ============================================
# CORS Configuration
# ============================================

# Allowed origins (comma-separated)
# ALLOWED_ORIGINS=http://localhost:3000,https://yourapp.com

# ============================================
# Security Notes
# ============================================

# PRODUCTION CHECKLIST:
# 1. Generate a strong JWT_SECRET (64+ characters)
# 2. Use shorter token expiration times
# 3. Enable HTTPS only
# 4. Set up proper CORS origins
# 5. Use environment-specific .env files
# 6. Never commit .env to version control
# 7. Consider using AWS Secrets Manager or similar

# Generate secure JWT secret:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

# Or use this PowerShell command:
# -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 64 | ForEach-Object {[char]$_})
